openapi: 3.0.3
info:
  title: Dummy App Service API
  description: >
    Version 2 of the Dummy App Service API.
    This release introduces breaking changes, stricter authentication,
    and a redesigned order model.
  version: 1.0.0
  contact:
    name: Dummy API Team
    email: api@dummyapp.dev

servers:
  - url: https://api.dummyapp.dev/v2
    description: Production server (v2)

tags:
  - name: Users
    description: User management APIs
  - name: Orders
    description: Order processing APIs
  - name: Health
    description: Service health APIs

security:
  - ApiKeyAuth: []

paths:
  /users:
    post:
      tags:
        - Users
      summary: Create a new user
      operationId: createUserV2
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateUserRequest"
      responses:
        "201":
          description: User created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          description: Unauthorized

  /users/{userId}:
    get:
      tags:
        - Users
      summary: Get user details
      operationId: getUserV2
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: User details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "401":
          description: Unauthorized
        "404":
          description: User not found

  /orders:
    post:
      tags:
        - Orders
      summary: Create an order
      operationId: createOrderV2
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateOrderRequestV2"
      responses:
        "201":
          description: Order created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderV2"
        "400":
          description: Invalid order data
        "401":
          description: Unauthorized

  /orders/{orderId}:
    get:
      tags:
        - Orders
      summary: Get order details
      operationId: getOrderV2
      parameters:
        - name: orderId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Order details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/OrderV2"
        "401":
          description: Unauthorized
        "404":
          description: Order not found

  /health:
    get:
      tags:
        - Health
      summary: Health check
      operationId: healthCheck
      security: []
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-API-Key

  schemas:
    CreateUserRequest:
      type: object
      required:
        - email
        - name
      properties:
        email:
          type: string
          format: email
        name:
          type: string

    User:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
        name:
          type: string
        createdAt:
          type: string
          format: date-time

    CreateOrderRequestV2:
      type: object
      required:
        - customerId
        - totalAmount
      properties:
        customerId:
          type: string
          example: usr_12345
        totalAmount:
          type: number
          minimum: 0.01
          example: 249.99
        currency:
          type: string
          example: USD

    OrderV2:
      type: object
      properties:
        id:
          type: string
        customerId:
          type: string
        totalAmount:
          type: number
        currency:
          type: string
        status:
          type: string
          enum:
            - CREATED
            - CONFIRMED
            - SHIPPED
            - COMPLETED
            - CANCELLED
        createdAt:
          type: string
          format: date-time

    HealthStatus:
      type: object
      properties:
        status:
          type: string
          example: ok
        timestamp:
          type: string
          format: date-time